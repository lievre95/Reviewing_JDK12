/*
package OReilly.other;
import animal.*;
public class Tadpole extends Frog {
    public static void main(String[] args) {
        Tadpole t =new Tadpole(); // instance of Tadpole is not in the same package as Frog
        t.ribbit(); //here is ok; ribbit() is protected access which means it can only be accessed from a subclass reference or in same package
//        t.jump(); //default access modifier allows a method to be accessed only from the same package
        Frog f = new Tadpole();
        f.ribbit(); //here is NOT OK; ribbit() is protected access which means it can only be accessed from a subclass reference or in same package
//        f.jump();  // --
    }
}
*/
